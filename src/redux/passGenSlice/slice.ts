import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const passGenSlice = createSlice({
    name: 'passGen',
    initialState,
    reducers: {

    }
})

export const {} = passGenSlice.actions
export default passGenSlice.reducer